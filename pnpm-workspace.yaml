nodeLinker: hoisted

#  prevent pnpm from creating a lockfile, hence it conflicts with the npm lockfile
lockfile: false
pmOnFail: ignore

allowBuilds:
  esbuild: true
  unrs-resolver: true
minimumReleaseAgeExclude:
  - '@vitest/mocker@5.0.1'
  - '@vitest/spy@5.0.1'
  - vitest@5.0.1
